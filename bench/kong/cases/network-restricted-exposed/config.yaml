# Authentication alone does not enforce the network boundary: an authenticated
# request from outside the trusted CIDR remains reachable.
services:
  - name: internal-api
    routes:
      - name: internal-get
        paths: [/internal]
        methods: [GET]
        hosts: [internal.example]
        plugins: [{name: key-auth}]
