(test
 (name corpus)
 (modules corpus)
 (libraries soundcheck_core soundcheck_kong)
 (deps
  (source_tree kong/cases)))

; Ad-hoc survey of external decK configs — not a gate, run by hand against a
; directory of real configs. See the header of survey.ml.
(executable
 (name survey)
 (modules survey)
 (libraries soundcheck_core soundcheck_kong yaml))

; Concrete oracle used by the opt-in differential test against a real Kong.
; This is deliberately not a dune test: Docker is not part of the unit-test gate.
(executable
 (name kong_model_oracle)
 (modules kong_model_oracle)
 (libraries soundcheck_core soundcheck_kong))
